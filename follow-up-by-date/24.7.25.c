#include <stdio.h>
#include <math.h> 
/*
void lidyNum( int num,int sz)
{
	int sum=0;
	int j;
	int numTemp=num;
	for(j=1;j<=sz;j++)
	{
		sum+=pow(numTemp%10,sz);
		numTemp/=10;
	}
	if(sum==num)
		printf("%d\n",num);
}

int main() 
{
	int i;
	for(i=0;i<100001;i++)
	{	
		int count=1;
		int i_=i;
		while(i_/=10)
		{
			;
			count++;
		}
		lidyNum(i,count);		
	}
	return 0;
}
*/
